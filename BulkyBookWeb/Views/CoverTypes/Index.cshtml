@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Cover Types";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary text-start">Cover Types</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle me-2"></i> Create New Cover Type
            </a>
        </div>
    </div>
</div>

<table class="table table-striped table-bordered w-100">
    <thead>
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var coverType in Model)
        {
            <tr>
                <td width="80%">@coverType.Name</td>
                <td>
                    <div class="btn-group w-75" role="group">
                        <a asp-action="Edit" asp-route-id="@coverType.Id" class="btn btn-primary mx-2" role="button">
                            <i class="bi bi-pencil-square me-2"></i>Edit
                        </a>
                        <a asp-action="Delete" asp-route-id="@coverType.Id" class="btn btn-danger mx-2" role="button">
                            <i class="bi bi-trash me-2"></i>Delete
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<partial name="_Notification"></partial>

